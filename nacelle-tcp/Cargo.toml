[package]
name = "nacelle-tcp"
version = "0.1.0"
edition = "2024"

[features]
default = []

[dependencies]
bytes = "1.10.1"
nacelle-core = { path = "../nacelle-core" }
tokio = { version = "1.47.1", features = ["io-util", "macros", "net", "rt", "rt-multi-thread", "sync", "time"] }
tracing = "0.1.44"
